<?php

// Jonathan Isay Bernal Arriaga
// 2024 Ene-Jun

$cases = trim(fgets(STDIN));

for ($iterate=0; $iterate<$cases; $iterate++){
    $process = trim(fgets(STDIN));
    $jar_proc = explode(' ', $process);
}

print_r($jar_proc);

function jar_filling(){
    $filled = 0;
    $extra = 0;

    for ($iterate=1; $iterate<=$jar_proc[0]; $iterate++) {

    }
}

?>